apply plugin: 'com.android.application'

android {
    signingConfigs {
        hazenandroiddev {
            keyAlias 'hazenandroiddev'
            keyPassword '1@Icapell'
            storeFile file('C:/Users/Hazen/.AndroidStudioBeta/keystore.jks')
            storePassword '1@Icapell'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.1"
    defaultConfig {
        applicationId "com.dct.testLocation"
        minSdkVersion 15
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"
        signingConfig signingConfigs.hazenandroiddev
    }
    buildTypes {
        release {
            runProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.google.android.gms:play-services:6.1.+'
    compile 'com.google.maps.android:android-maps-utils:0.3+'
}
